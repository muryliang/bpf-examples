git commit -m "now can wor, even without bridge, not know why, maybe something with environment, next maybe multithread handle different direction flow, and multiqueue(not ordered"

but first time arp not work, need some query
    turns out the bridge has side effect, even only set as master and not link up, and after first success, we can disable bridge module, and still work, should know why
